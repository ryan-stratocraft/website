# Optional Vite env vars (see vite.config.ts / import.meta.env)
#
# Firebase Web SDK config for strato-craft-6c348 is inlined in:
#   src/firebase/firebase.ts
#
# Comma-separated emails allowed to sign in to /signature (must match storage.rules):
# VITE_SIGNATURE_ALLOWED_EMAILS=ryan@strato-craft.com,other@strato-craft.com
